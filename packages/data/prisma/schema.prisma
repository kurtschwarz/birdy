generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Recorder {
  id   String  @id @default(uuid())

  @@map("recorders")
}
